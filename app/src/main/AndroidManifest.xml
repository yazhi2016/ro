<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.xmzlb.iron">

    <!-- 高德地图start -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <!-- 高德地图end -->
    <application
        android:name=".base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity"
            android:windowSoftInputMode="adjustPan|stateHidden"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activity.ProductInfoActivity">
        </activity>
        <activity
            android:name=".activity.LocationActivity"
            android:windowSoftInputMode="adjustPan|stateHidden">
        </activity>

        <!-- 高德地图start -->
        <service android:name="com.amap.api.location.APSService">
        </service>

        <meta-data
            android:name="com.amap.api.v2.apikey"
            android:value="d8fa8028e5e74d5f500c121236164109"/>
        <!-- 高德地图end -->

        <activity android:name=".activity.MyOrderActivity">
        </activity>
        <activity android:name=".activity.LookLogisticsActivity">
        </activity>
        <activity android:name=".activity.RobOrderDetailActivity">
        </activity>
        <activity android:name=".activity.OrderDetailActivity">
        </activity>
        <activity
            android:name=".activity.LogisticsAssessActivity"
            android:windowSoftInputMode="adjustPan|stateHidden">
        </activity>
        <activity android:name=".activity.CarDetailActivity">
        </activity>
    </application>

</manifest>